import React from 'react'
import PropTypes from 'prop-types'

function Modal({}) {
  return (
    <div className=' fixed top-0 left-0 w-full h-full bg'>Modal</div>
  )
}

Modal.propTypes = {}

export default Modal
